# สรุปไฟล์ที่สร้างและการใช้งาน

## ✅ ไฟล์ที่สร้างเสร็จแล้ว

### 1. Database & Configuration

- ✅ `supabase-schema.sql` - SQL Schema สำหรับสร้างตารางใน Supabase
- ✅ `.env.example` - ตัวอย่างไฟล์ environment variables
- ✅ `src/lib/supabase.ts` - Supabase client configuration
- ✅ `src/types/supabase.ts` - TypeScript types สำหรับ Database
- ✅ `src/types/index.ts` - Types เพิ่มเติม

### 2. Services (API Layer)

- ✅ `src/services/orderService.ts` - จัดการออเดอร์
- ✅ `src/services/userService.ts` - จัดการลูกค้า
- ✅ `src/services/productService.ts` - จัดการสินค้า
- ✅ `src/services/paymentService.ts` - จัดการการชำระเงิน
- ✅ `src/services/reportService.ts` - รายงานและสรุปต่างๆ

### 3. Views (หน้าจอหลัก)

- ✅ `src/views/Tab1Page.vue` - ระบบออเดอร์/สั่งจองรายวัน
- ✅ `src/views/Tab2Page.vue` - สรุปยอดรายวัน
- ✅ `src/views/Tab3Page.vue` - ลูกค้า & ค้างชำระ
- ✅ `src/views/TabsPage.vue` - Navigation tabs (อัพเดทแล้ว)
- ✅ `src/views/InventoryPage.vue` - คลังสินค้า/สต็อก (พร้อมใช้งาน)

### 4. Documentation

- ✅ `README.md` - คู่มือการใช้งานฉบับสมบูรณ์
- ✅ `install-deps.sh` - สคริปต์ติดตั้ง dependencies
- ✅ `SETUP.md` - ไฟล์นี้

## 📋 ขั้นตอนการติดตั้งและใช้งาน

### Step 1: ติดตั้ง Dependencies

```bash
# ใช้สคริปต์อัตโนมัติ (macOS/Linux)
chmod +x install-deps.sh
./install-deps.sh

# หรือติดตั้งด้วยตนเอง
npm install @supabase/supabase-js
```

### Step 2: ตั้งค่า Supabase

1. ไปที่ [https://supabase.com](https://supabase.com)
2. สร้าง New Project
3. รอให้ Database พร้อม (ประมาณ 2-3 นาที)
4. ไปที่ **SQL Editor**
5. สร้าง New Query และ copy ข้อมูลจาก `supabase-schema.sql`
6. กด **Run** เพื่อสร้างตารางทั้งหมด

### Step 3: ตั้งค่า Environment Variables

```bash
# คัดลอกไฟล์ตัวอย่าง
cp .env.example .env
```

แก้ไขไฟล์ `.env`:

1. ไปที่ Supabase Dashboard → Settings → API
2. คัดลอก **Project URL** และ **anon/public key**
3. ใส่ใน `.env`:

```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Step 4: รันแอปพลิเคชัน

```bash
# Development mode
npm run dev

# หรือใช้ Ionic CLI
ionic serve
```

เปิดบราวเซอร์ที่ `http://localhost:5173` (Vite) หรือ `http://localhost:8100` (Ionic)

## 🎯 การใช้งานแต่ละฟีเจอร์

### Tab 1: ระบบออเดอร์

1. กด **"สร้างออเดอร์"** เพื่อเพิ่มออเดอร์ใหม่
2. เลือกลูกค้า, สินค้า, จำนวน
3. ระบบจะคำนวณราคาอัตโนมัติ
4. เลือกวิธีการชำระเงินและสถานะ
5. สไลด์ซ้าย-ขวาที่รายการเพื่อ:
   - ชำระแล้ว (ถ้ายังค้างชำระ)
   - แก้ไข
   - ยกเลิก

### Tab 2: สรุปรายวัน

1. เลือกวันที่ต้องการดูรายงาน
2. ดูสรุป:
   - จำนวนออเดอร์และถังที่ขาย
   - ยอดขายรวม
   - เงินสดที่ได้รับ
   - ยอดค้างชำระ
   - กำไรประมาณการ
3. ดูรายการค้างชำระใหม่ของวันนั้น
4. ดูแยกตามวิธีการชำระเงิน

### Tab 3: ลูกค้า & ค้างชำระ

#### แท็บค้างชำระ:

1. ดูรายการค้างชำระทั้งหมด
2. เรียงตามวันที่ค้างนานสุด
3. สไลด์ซ้ายเพื่อ:
   - ชำระแล้ว
   - ติดต่อ (โทรออก)

#### แท็บรายชื่อลูกค้า:

1. ค้นหาลูกค้าด้วยชื่อหรือเบอร์โทร
2. กรองตามสถานะ
3. กด **"เพิ่มลูกค้าใหม่"**
4. สไลด์ซ้ายที่ลูกค้าเพื่อ:
   - ดูประวัติ
   - แก้ไข
   - ระงับบัญชี

### หน้าคลังสินค้า (InventoryPage.vue)

**หมายเหตุ:** หน้านี้สร้างแล้วแต่ยังไม่ได้เพิ่มใน router

ต้องการเพิ่มเข้า router:

1. เปิดไฟล์ `src/router/index.ts`
2. เพิ่ม route ใหม่:

```typescript
{
  path: 'inventory',
  component: () => import('@/views/InventoryPage.vue')
}
```

3. เพิ่มแท็บใน `TabsPage.vue`:

```vue
<ion-tab-button tab="inventory" href="/tabs/inventory">
  <ion-icon :icon="cubeOutline" />
  <ion-label>คลังสินค้า</ion-label>
</ion-tab-button>
```

## 🔧 ฟีเจอร์ที่ทำงานอัตโนมัติ

1. **เช็คสต็อก**: ก่อนสร้างออเดอร์จะเช็คว่าสต็อกเพียงพอ
2. **ลดสต็อก**: เมื่อสร้างออเดอร์สำเร็จ จะลดสต็อกอัตโนมัติ
3. **คืนสต็อก**: เมื่อยกเลิกออเดอร์ จะคืนสต็อกอัตโนมัติ
4. **บันทึกประวัติ**: ทุกการเปลี่ยนแปลงสต็อกจะบันทึกลง `inventory` table
5. **อัพเดทรายงาน**: Daily report จะอัพเดทอัตโนมัติผ่าน Database Trigger
6. **แจ้งเตือนสต็อกต่ำ**: แสดงเตือนเมื่อสต็อกต่ำกว่าที่กำหนด

## 📊 ข้อมูลตัวอย่าง

ไฟล์ `supabase-schema.sql` มีข้อมูลตัวอย่าง:

### สินค้า:

- ถังน้ำดื่ม 20 ลิตร (25฿)
- ขวดน้ำดื่ม 600ml (7฿)
- ขวดน้ำดื่ม 1.5 ลิตร (12฿)

### ลูกค้า:

- คุณสมชาย ใจดี
- ร้านกาแฟดีดี
- บริษัท ABC จำกัด

## 🐛 การแก้ปัญหา

### ปัญหา: Cannot connect to Supabase

**วิธีแก้:**

1. ตรวจสอบ `.env` file ว่ามี URL และ Key ถูกต้อง
2. ตรวจสอบว่ารัน `npm run dev` ใหม่หลังแก้ไข `.env`

### ปัญหา: No data showing

**วิธีแก้:**

1. ตรวจสอบว่ารัน SQL Schema แล้ว
2. เช็คว่ามีข้อมูลตัวอย่างใน Supabase Dashboard → Table Editor

### ปัญหา: Stock not updating

**วิธีแก้:**

1. ตรวจสอบ Database Triggers ใน Supabase
2. ตรวจสอบ SQL Schema ว่ารันครบทุก function

## 📱 Build Mobile App

```bash
# iOS
ionic capacitor add ios
ionic capacitor build ios
ionic capacitor open ios

# Android
ionic capacitor add android
ionic capacitor build android
ionic capacitor open android
```

## 🚀 Deploy

### Vercel/Netlify:

```bash
npm run build
# Deploy folder 'dist'
```

### Firebase Hosting:

```bash
npm install -g firebase-tools
firebase login
firebase init
npm run build
firebase deploy
```

## 📝 Next Steps

1. เพิ่มหน้าคลังสินค้าเข้า router
2. สร้างหน้ารายงานรายเดือน/รายปี
3. เพิ่ม Export เป็น PDF/Excel
4. เพิ่ม Dashboard กราฟ
5. Authentication (Login/Register)

## 🤝 Support

หากมีปัญหาหรือข้อสงสัย:

1. ตรวจสอบ Console (F12) ดู error
2. ตรวจสอบ Supabase Dashboard → Logs
3. อ่าน [Supabase Documentation](https://supabase.com/docs)
4. อ่าน [Ionic Vue Documentation](https://ionicframework.com/docs/vue/overview)
